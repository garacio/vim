
```
# git clone https://github.com/garacio/vim.git .vim
# cd ~/.vim
# git submodule sync --recursive
# git submodule update --init --recursive
# ln -s ~/.vimrc ~/.vim/vimrc 

# vim +PluginInstall +qall
```


